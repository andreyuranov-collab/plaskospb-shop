# CHANGELOG

## v00.05_test_260707
- Убран пункт «Преимущества» из навигации хедера.
- Убран первый блок на главной после hero: горизонтальная плашка популярных материалов PLA / PETG / ABS и т.д.
- Каталог, доставка, оптовая форма и корзина-заявка не изменялись.

# Changelog

## v00.02_test_260707

- Удалён `package.json`, чтобы Cloudflare не запускал bun/npm и не воспринимал проект как Node/Worker.
- Удалён `.gitignore`, так как для загрузки через GitHub Upload он не нужен.
- Переписана инструкция деплоя: основной сценарий — GitHub Upload → Cloudflare Pages.
- Явно указано: `npx wrangler deploy` не использовать.
- Для Cloudflare Pages рекомендованы настройки `Build command: exit 0`, `Build output directory: .`.

## v00.01_test_260707

- Первая тестовая версия статического магазина-заявки.

## v00.03 test — 2026-07-07
- Исправлен SVG-логотип: вместо ошибочного визуального окончания `IP` теперь явно отображается `SPB`.
- Юридическая форма не утверждается и не подставляется автоматически.

## v00.04_test_260707

- Исправлен клик по логотипу в хедере: теперь ссылка не якорится на sticky-header и возвращает страницу строго в начало.
- Добавлен обработчик `data-home-link`, который закрывает мобильное меню/корзину, очищает hash и выполняет `scrollTo(0, 0)`.
- Добавлен отдельный скрытый якорь `#page-top`; `id` убран со sticky-header, чтобы браузер не прокручивал страницу на несколько пикселей вниз.
